import {} from '../../redux/actionConstants'

const initialState = {}

export function generatorReducer(state = initialState, action: { type: string }){
    switch(action.type){
        default:
            return state
    }
}